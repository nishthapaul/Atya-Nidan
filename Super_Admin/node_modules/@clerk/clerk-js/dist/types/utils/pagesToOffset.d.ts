import type { ClerkPaginationParams } from '@clerk/types';
type Pages = {
    initialPage?: number;
    pageSize?: number;
};
export declare function convertPageToOffset<T extends Pages | undefined>(pageParams: T): ClerkPaginationParams;
export {};
